# handover-item-basic

这是官方 `Debut` 中 `HandoverItem` 条件的摘录样例。

用途：

- 看 `HandoverItem` 的原生字段写法
- 看 `target`、`value`、`onlyFoundInRaid` 这类字段该怎么组织

注意：

- 这是官方结构摘录，不是可直接安装的 VisitAPI 任务包
- 真正生成自定义任务时，仍需补全完整 quest 顶层字段与 locale
