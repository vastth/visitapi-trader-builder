# kill-counter-basic

这是官方 `Debut` 中 `CounterCreator` 条件的摘录样例。

用途：

- 看击杀类任务的 `counter.conditions` 结构
- 看 `Kills` 条件如何放在 `CounterCreator` 下面

注意：

- 复杂击杀条件不要只照抄这一条
- 生成新任务前仍要结合 `official_quest_examples.json` 与真实官方 quest 再核对一次
