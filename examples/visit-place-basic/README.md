# visit-place-basic

这是官方 `Secret Benefactor` 中 `VisitPlace` 条件的摘录样例。

用途：

- 看 `VisitPlace` 的最小字段结构
- 作为到达类任务的起手参考

注意：

- `VisitPlace` 的 `target` 含义与具体任务实现相关，不要随意猜值
- 需要 zoneId 或更复杂位置条件时，回查 `indexes/map_zone_index.json` 与 `sources/`
