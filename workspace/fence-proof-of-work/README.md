# workspace / fence-proof-of-work

这是当前开发工作区中的 Fence 首任务闭环。

包含：

- `579dc571d53a0658a154fbec.dlg`
- `fence-proof-of-work.json`
- `ch.json`

用途：

- 作为当前可编辑工作副本
- 在不污染 `examples/` 的前提下继续迭代

如果需要英文 locale、背景图、任务图标，可从 `examples/fence-proof-of-work/` 复制对应文件。
